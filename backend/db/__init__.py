# db package — database clients
# This folder contains only mongo_client.py because:
# - Qdrant is managed by LangChain's QdrantVectorStore
# - Elasticsearch is managed by LangChain's ElasticsearchStore
# - MongoDB is the only database we talk to directly (for status tracking)
